# Archived Route Files

These files are **NOT active** — they are kept here for reference only and are not imported anywhere in the router.

Each was replaced by a newer version in a subdirectory. Do not re-import these; use the active replacements listed below.

| Archived File | Active Replacement | Reason |
|---|---|---|
| `auth.ts` | `../system/auth.ts` | Moved to `system/` subdirectory |
| `rides.ts` | `../fleet/rides.ts` | Moved to `fleet/` subdirectory |
| `service-zones.ts` | `../fleet/zones.ts` | Moved to `fleet/` subdirectory, renamed |
| `users.ts` | `../system/users.ts` | Moved to `system/` subdirectory |

## To Restore

If you ever need to recover logic from one of these files, copy the relevant code into the active file (do not re-mount the archived file directly).
